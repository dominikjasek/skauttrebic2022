query GetComments($postId: String!) {
    findAllFlat(relation: $postId, pagination: {pageSize: 1000}) {
        data {
            id
            content
            blocked
            author {
                id
                name
                email
                avatar
            }
            createdAt
        }
    }
}
