query GetComments($postId: String!) {
    findAllFlat(relation: $postId) {
        data {
            id
            content
            blocked
            author {
                id
                name
                email
                avatar
            }
            createdAt
        }
    }
}
